
name: flutter_mvvm_app
description: Flutter app with API, ListView, navigation, loading/error states
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.1

flutter:
  uses-material-design: true
